# Measuring Spatial Balance

BiodiversityObservationMethods contains a few utilities for quantifying the spatial balance of a set of nodes in a [`BiodiversityObservationNetwork`].

These methods are all used by calling the [`spatialbalance`] method

```@docs; canonical=false
spatialbalance
```